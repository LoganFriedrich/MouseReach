# MouseReach Improvement Process (Signpost)

This directory is a signpost only. It does not contain code or data.

- **Code** lives at `src/mousereach/improvement/` (in the MouseReach git repo).
- **Output data** (snapshots) accumulates at
  `MouseReach_Pipeline/Improvement_Snapshots/` (data-only, not in git).

See `src/mousereach/improvement/README.md` for full documentation on how
to create snapshots, add phases, and add figure types.
